# UI mockups (reference)

## Reference image

- **`reference-cue-structure-energy-dark.png`** — Screenshot of the dark “Cue Structure & Energy” concept (waveform-linked section overlays + right detail panel). Use as a visual north star when aligning `FileAnalysis` and related views.

## Full-page mockup (static HTML)

- **`../../frontend/public/file-analysis-full-mockup.html`** — Standalone mockup of the entire analysis page in the same dark design language (header, toolbar, waveform + structure block, detection settings, analysis results grid, footer). Not wired to the app.

Open with the dev server:

`http://localhost:5174/file-analysis-full-mockup.html`

Or open the HTML file directly from disk.
